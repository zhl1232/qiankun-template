{
  "name": "main",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "vue-tsc --noEmit && pnpm lint:eslint && pnpm lint:stylelint"
  },
  "dependencies": {}
}
